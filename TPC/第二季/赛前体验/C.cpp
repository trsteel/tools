#include <iostream>
#include <cstring>
#include <algorithm>
#include <vector>

using namespace std;

const long maxn = 1e5 + 1;
const long maxa = 1e3 + 1;

long t, n, a[maxn], cnt[maxa];


int main() {
    cin>>t;
    while(t--) {
        cin>>n;
        
        if(t>0) cout<<endl;
    }
    return 0;
}